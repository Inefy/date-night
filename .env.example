# Supabase public client config. The anon key is safe in client builds only when RLS is enforced.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Never add service role keys or other privileged secrets to Expo client env files.
